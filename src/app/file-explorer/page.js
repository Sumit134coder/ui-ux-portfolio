import FileExp from "@/views/File-explorer"

export const metadata = {
    title: 'File Explorer',
    description: 'A file system'
}

const FileExplorer = () => {
  return (
    <FileExp/>
  )
}

export default FileExplorer